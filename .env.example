# ==================================
# Learn Vanguard Environment Variables
# ==================================
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control!

# ==================================
# API Configuration
# ==================================
# Backend API base URL (with /api path)
# Development: http://localhost:5000/api
# Production: https://learn-vanguard-server.onrender.com/api
VITE_API_BASE_URL=http://localhost:5000/api

# Backend base URL (without /api path)
# Development: http://localhost:5000
# Production: https://learn-vanguard-server.onrender.com
VITE_BACKEND_URL=http://localhost:5000

# ==================================
# Firebase Configuration
# ==================================
# Get these from Firebase Console > Project Settings > General
# https://console.firebase.google.com/

# Firebase API Key (public key, safe to expose in client code)
VITE_FIREBASE_API_KEY=your_firebase_api_key_here

# Firebase Auth Domain (e.g., your-project.firebaseapp.com)
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# Firebase Project ID
VITE_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket (e.g., your-project.appspot.com)
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Firebase Messaging Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id

# Firebase App ID
VITE_FIREBASE_APP_ID=your-firebase-app-id

# ==================================
# WebSocket Configuration
# ==================================
# WebSocket URL for real-time features
# Development: ws://localhost:5000/ws
# Production: wss://learn-vanguard-server.onrender.com/ws
VITE_WS_URL=ws://localhost:5000/ws

# ==================================
# Development Settings
# ==================================
# Use mock authentication (bypasses Firebase) - for testing only
# Values: "true" | "false"
VITE_USE_MOCK_AUTH=false

# Use mock data instead of API calls - for development/testing
# Values: "true" | "false"
VITE_USE_MOCK_DATA=false

# Enable automatic token refresh functionality
# Values: "true" | "false"
VITE_ENABLE_TOKEN_REFRESH=true

# ==================================
# File Upload Configuration
# ==================================
# Maximum file size in bytes (default: 10485760 = 10MB)
# Examples:
#   5MB = 5242880
#   10MB = 10485760
#   20MB = 20971520
VITE_MAX_FILE_SIZE=10485760

# Allowed file types (comma-separated, no spaces)
# Default: pdf,doc,docx,ppt,pptx,xls,xlsx,txt,jpg,jpeg,png
VITE_ALLOWED_FILE_TYPES=pdf,doc,docx,ppt,pptx,xls,xlsx,txt,jpg,jpeg,png

# ==================================
# Notes
# ==================================
# 1. Firebase API keys are meant to be public but should be restricted by:
#    - Domain restrictions (configured in Firebase Console)
#    - Firebase Security Rules
#    - App Check (optional, for production)
#
# 2. For production deployment, ensure:
#    - All URLs use HTTPS/WSS protocols
#    - Firebase security rules are properly configured
#    - Backend API has proper CORS configuration
#
# 3. The application uses Vite's environment variable system:
#    - Variables must start with VITE_ to be exposed to client
#    - Access via import.meta.env.VARIABLE_NAME
#    - See: https://vitejs.dev/guide/env-and-mode.html
